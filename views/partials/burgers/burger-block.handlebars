<li>
    {{burger_name}}

    <button class="devour-burger" data-id="{{id}}" data-devoured="{{devoured}}">
        Devour thie burger!
    </button>
    <button class="delete-burger" data-id="{{id}}">
        Delete this burger
    </button>
</li>